# Only track agents, commands, CLAUDE.md, and transition plan
# Ignore everything else (local state, caches, sessions, etc.)
*

# Include tracked files
!.gitignore
!CLAUDE.md
!agents/
!agents/**
!commands/
!commands/**
!projects/projects.yaml
