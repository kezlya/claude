# Only track agents, commands, and transition plan
# Ignore everything else (local state, caches, sessions, etc.)
*

# Include agents and commands
!.gitignore
!agents/
!agents/**
!commands/
!commands/**
!2026-03-20-global-agent-transition.md
